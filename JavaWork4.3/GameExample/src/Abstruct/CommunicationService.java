package Abstruct;

import Entity.Communication;

public interface CommunicationService {
	
	void Add(Communication communicationService);
	void Update(Communication communicationService);
	void Delete(Communication communicationService);
}
